# frozen_string_literal: true

source "https://rubygems.org"

gemspec path: "../.."

gem "railties", "5.2.8.1"

if RUBY_VERSION >= "3.4"
  gem "base64" # Fix (cannot load such file -- base64)
  gem "bigdecimal" # Fix (cannot load such file -- bigdecimal)
  gem "mutex_m" # Fix (cannot load such file -- mutex_m)
end

if RUBY_VERSION >= "4.0"
  gem "irb" # Fix (cannot load such file -- irb)
  gem "logger" # Fix (cannot load such file -- logger)
end

# development dependencies
gem "rspec"
gem "simplecov"
